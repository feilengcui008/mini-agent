# Introduction

A very simple agent for learning, support cli ui, tool use, session management, context compression, subagent etc.
Currently only implement claude compatible api and tested with minimax api, usage:

```
./target/debug/mini-agent --api-key={minimax_api_key}
```
